# Orchestration Log — Gimli

**Date:** 2026-02-18
**Session:** Irish Hold 'Em Phase 2 (Staged Deploy)

## Task

Fixed `TableStateBuilder.cs` for Irish Hold 'Em showdown evaluation.

## Changes

- Added `IsIrishHoldEmGame()` convenience method alongside existing `IsHoldEmGame()`/`IsOmahaGame()` helpers.
- Added dedicated Irish Hold 'Em showdown block after the Omaha block — loads community cards from DB, creates `HoldemHand`, finds best 5-of-7 hand.
- Without this fix, post-discard Irish players (2 hole cards) fell through to the generic handler which never loaded community cards, causing players to be skipped at showdown.

**File:** `src/CardGames.Poker.Api/Services/TableStateBuilder.cs`
