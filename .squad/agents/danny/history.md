# Danny History

## Project Learnings (from import)
- Project stack: C#/.NET (`net10.0`) with API services and domain modules.
- Requested by: Rob Gibbens.
- Key backend areas: src/CardGames.Poker.Api, src/CardGames.Poker, src/CardGames.Core.

## Learnings
- 2026-02-17 (Leagues design review): Keep Leagues as account-scoped social orchestration (membership/admin/invites/schedule) and keep poker rules/hand flow outside this boundary.
- 2026-02-17 (Leagues design review): Use append-only membership/admin events with current-state projections to support temporal membership, auditability, and safer concurrency handling.
- 2026-02-17 (Leagues design review): MVP invite links should be revocable + expiring token URLs validated server-side, with join idempotency and hashed token storage.
- 2026-02-17 (Leagues design review): MVP supports both season containers and one-off events as scheduling primitives; standings/leaderboards are post-MVP.
- Scribe merged Leagues design-review and backend-design decisions into canonical `.ai-team/decisions.md`; treat that file as the team-approved MVP contract baseline.
- 2026-02-18 (#216/#224 sync): backend Leagues v1 governance endpoints (transfer ownership, demote admin, remove member) and new membership-history values are now canonicalized in `.ai-team/decisions.md`; API changes should preserve manager/governance-capable invariants and existing moderation/idempotency response semantics expected by integration gates.
- 2026-02-19 (league invite permissions): keep persisted creator membership as `Owner` for governance/audit semantics, but project current-user `Owner` as `Manager` in create/detail/my-leagues responses so management-capable clients remain aligned with invite authorization checks and newly created leagues can create invites immediately from detail flows.
- 2026-02-19 (league invite permissions follow-up): client gating for invite/management actions should treat `Owner` as management-capable alongside `Manager`/`Admin` to stay resilient if any response path returns unprojected role values.
- 2026-02-19 (team update): #216 authority model is now finalized—active admins and managers can execute admin promote/demote operations; ownership transfer remains manager-only with governance safety invariants unchanged.
- 2026-03-02 (Dealer's Choice design): Produced comprehensive API + DB schema design for Dealer's Choice tables. Key files analyzed: `Game.cs` (entity), `GameType.cs`, `CreateGameCommand.cs`/Handler, `ContinuousPlayBackgroundService.cs`, `StartHandCommandHandler.cs`, `IGameFlowHandler.cs`/Factory, `GameHub.cs`, `GameStateBroadcaster.cs`, `TableStatePublicDto.cs`, `Phases.cs`, `PokerGameMetadataRegistry.cs`.
- 2026-03-02 (Dealer's Choice design): Core approach: nullable `GameTypeId` on `Game`, new `WaitingForDealerChoice` phase, `ChooseDealerGameCommand` MediatR command, separate `DealersChoiceDealerPosition` for multi-hand game support, `DealersChoiceHandLog` entity for per-hand audit trail, DC branch in `ContinuousPlayBackgroundService`.
- 2026-03-02 (Dealer's Choice design): Backward compat guaranteed — existing tables always have `IsDealersChoice = false` + non-null `GameTypeId`. All existing flows guarded by early-return check. Phase-based blocking (`WaitingForDealerChoice`) reuses existing action timer/broadcast infrastructure.
- 2026-03-02 (Dealer's Choice design): Multi-hand games (Kings and Lows): `DealersChoiceDealerPosition` stays fixed while sub-hands rotate `DealerPosition`. DC position advances only after full game resolution (showdown with pot awarded, not carried-over pots).
- 2026-03-02 (Dealer's Choice Phase 1 — schema): Applied all DB schema changes: nullable `GameTypeId`/`GameType` on `Game.cs`, added `IsDealersChoice`, `CurrentHandGameTypeCode`, `DealersChoiceDealerPosition` properties. Created `DealersChoiceHandLog` entity + EF config. Added `WaitingForDealerChoice` to `Phases` enum. Made GameType FK optional in `GameConfiguration.cs`. Added `DealersChoiceHandLogs` DbSet to `CardsDbContext`.
- 2026-03-02 (Dealer's Choice Phase 1 — blockers): 3 build errors from nullable `GameTypeId`: `GetActiveGamesMapper.cs:16` (Guid? → Guid), `LobbyStateBroadcastingBehavior.cs:130` (Guid? → Guid), `GetGameMapper.cs:19` (Guid? → Guid). These must be fixed in Phase 2 before migration can be generated.
- 2026-03-05 (PRD 4.11 — DC modal blind support): Added optional `SmallBlind`/`BigBlind` to the full ChooseDealerGame pipeline: Request → Command → Endpoint → Handler → Successful response → DealersChoiceHandLog entity → Contracts DTO → DealerChoiceModal.razor → TablePlay.razor. Blind validation added in handler (must be > 0, BigBlind >= SmallBlind). Frontend conditionally renders blind fields when `HOLDEM` is selected via `IsBlindBasedGame()` helper. Game entity already had `SmallBlind`/`BigBlind` columns — no migration needed. Contracts types live in `CardGames.Poker.Api.Clients` namespace (`ChooseDealerGameExtensions.cs`), not the API namespace.
- 2026-03-06 (dashboard community-card odds): Root cause for wrong dashboard odds in community-card games was client-side odds calculation in `TablePlay.razor` using only private hand cards and draw-game fallback logic, without visible community cards. Fixed by introducing `DashboardHandOddsCalculator` (community-aware per game code), passing `_tableState.CommunityCards` into calculation, and recalculating when public table state updates so flop/board transitions immediately affect odds.
- 2026-03-06 (Omaha showdown hardening Phase 0): Generic showdown evaluation now loads shared `CardLocation.Community` cards for community-card games and passes full private-card sets into positional evaluators for Hold'em/Omaha, avoiding pre-truncation to two cards before evaluator rules execute. Added dedicated `OmahaHandEvaluator` registration (`[HandEvaluator("OMAHA")]`) so Omaha no longer falls back to draw evaluator. Unskipped and strengthened `OmahaShowdownHardeningTests` to require winner resolution based on all four Omaha private cards with exact-two-hole semantics enforced by `OmahaHand`.
- 2026-03-06 (cross-agent sync): Scribe merged Omaha Phase 0 decisions from Basher/Danny/Linus into `.squad/decisions.md` and captured a single Omaha Phase 0 session log entry for traceability.
- 2026-03-06 (Omaha max-player consistency): Canonical Omaha max players is `10`; keep `PokerGameMetadataAttribute` and `OmahaRules.CreateGameRules()` aligned to prevent API/rules drift between registry paths.
- 2026-03-06 (cross-agent sync): Scribe merged `danny-omaha-max-players.md` into `.squad/decisions.md`, so Omaha max-player canonicalization is now captured in canonical squad decisions.
- 2026-03-07 (Irish Hold 'Em Phase 0 — Domain + API): Implemented full domain + API layer for Irish Hold 'Em game variant. Created 14 new files across `CardGames.Poker` (domain: IrishHoldEmGame, IrishHoldEmGamePlayer, IrishHoldEmRules, IrishHoldEmShowdownResult, IrishHoldEmHandEvaluator) and `CardGames.Poker.Api` (flow: IrishHoldEmFlowHandler; endpoints: IrishHoldEmApiMapGroup, V1, Feature, ProcessDiscard command/handler/endpoint/error/success). Updated PokerGameMetadataRegistry with `IrishHoldEmCode` constant. Key architecture decisions: (1) Reuses `Phases.DrawPhase` enum for the discard phase, (2) IrishHoldEmFlowHandler overrides `ProcessDrawCompleteAsync` to return `Turn` instead of `SecondBettingRound`, (3) Uses `HoldemHand` (not `OmahaHand`) for showdown since players have 2 hole cards post-discard, (4) Evaluator registered as `[HandEvaluator("IRISHHOLDEM")]` using Hold'em-identical logic, (5) Added `HasFolded` convenience property on IrishHoldEmGamePlayer to satisfy existing tests. Both CardGames.Poker and CardGames.Poker.Api projects compile cleanly. Pre-existing test project errors in GameApiRouterTests.cs are unrelated.

- 2026-03-06 (Omaha Phase 1 validation audit): Create Table and Dealer’s Choice UI both treat `OMAHA` as a blind-based variant (SB/BB inputs), and web betting routes `OMAHA` through the Hold’Em betting endpoint (no Five Card Draw fallback). Generic showdown is Omaha-safe via `OmahaHandEvaluator` + `OmahaShowdownHardeningTests` (exact-two-hole semantics enforced). Primary gap: web start-hand for non-DC Omaha calls the generic start endpoint, which only transitions phase and does not deal/collect blinds; the generic start handler needs to invoke `flowHandler.DealCardsAsync(...)` for blind-based/community-card games (or Omaha needs a dedicated start endpoint) so the first Omaha hand actually deals 4 hole cards.

- 2026-03-06 (generic StartHand auto-deal for skip-ante variants): Updated the generic start-hand handler to mirror the Hold’em start flow by calling `flowHandler.DealCardsAsync(context, game, eligiblePlayers, now, cancellationToken)` after persisting the initial hand state when `flowHandler.SkipsAnteCollection` is true. This fixes Omaha/Hold’em starts from the web (generic endpoint) so blinds are posted and hole cards are dealt immediately, while ante-based games (CollectingAntes) remain unchanged.
- 2026-03-06 (join-game buy-in backend verification): Confirmed `JoinGameCommandHandler` already enforces wallet-gated joins for requested buy-ins (`TryDebitForBuyInAsync`) and rejects zero-balance players (`ZeroAccountBalance`) before seating; integration coverage in `JoinLeaveWalletCommandTests` already validates debit/ledger/seat invariants for sufficient, insufficient, and zero-balance paths. Applied only contract/endpoint metadata cleanup (removed stale TODOs, corrected `JoinGameRequest` default-doc mismatch to 100, and documented wallet validation bullets + 403 response metadata) with no runtime behavior changes.
- 2026-03-07 (Irish Hold 'Em showdown fix — TableStateBuilder): Added dedicated Irish Hold 'Em showdown block in `TableStateBuilder.EvaluateShowdownHands`. Without it, post-discard Irish players (2 hole cards) fell through to the generic handler which never loaded community cards → `cards.Count < 5` → players skipped entirely. Fix: new `isIrishHoldEm` flag, new showdown block (loads community cards from DB, creates `HoldemHand`, finds best 5-of-7), new `IsIrishHoldEmGame()` convenience method. Verified in-play hand descriptions already work correctly via count-based dispatch (4 cards pre-discard → Omaha path, 2 cards post-discard → HoldEm path). File: `src/CardGames.Poker.Api/Services/TableStateBuilder.cs`.- 2026-02-18 (Irish Hold 'Em Phase 2): Fixed Irish showdown in TableStateBuilder — added `IsIrishHoldEmGame()` + dedicated showdown block for community card loading. Part of Phase 2 staged deploy session. 0 build errors, 60/60 Irish unit tests, 21/21 Irish integration tests passing.
- 2026-03-07 (Irish Hold 'Em Phase 3 — showdown fix): Applied `UsesSharedCommunityCards()` fix in `PerformShowdownCommandHandler.cs` to include `IrishHoldEmCode`. Without this, the authoritative showdown path skipped community cards for Irish hands, silently dropping players from evaluation. Fix verified: 81 Irish tests pass.
- 2026-03-07 (Hold the Baseball — full implementation): Implemented complete "Hold the Baseball" game variant — Texas Hold 'Em with 3s and 9s as wild cards. Created 22 new files: domain layer (HoldTheBaseballGame, Rules, GamePlayer, ShowdownResult), hand evaluation (HoldTheBaseballHand extends CommunityCardsHand with BaseballWildCardRules + WildCardHandEvaluator, HoldTheBaseballHandEvaluator with [HandEvaluator("HOLDTHEBASEBALL")]), API layer (FlowHandler, ApiMapGroup, Feature, V1, StartHand/ProcessBettingAction/PerformShowdown command folders each with Command/Successful/Handler/Endpoint). Integration: added `HoldTheBaseballCode` constant to PokerGameMetadataRegistry, added HOLDTHEBASEBALL to Generic showdown `UsesSharedCommunityCards()`. Key design decision: reuses `HandTypeStrengthRanking.Classic` (not a new ranking) since `HandTypeStrength` already supports FiveOfAKind at value 9. Wild card evaluation is delegated to the hand object itself via `WildCardHandEvaluator.EvaluateBestHand()`. Build verified clean (0 new errors).
- 2026-03-07 (Hold the Baseball blind-state verification): Blind posting occurred before deal, but `DealDrawStyleCardsAsync` reset `CurrentBet`/opening round state to zero. Fixed in `BaseGameFlowHandler` by preserving blind `CurrentBet` for skip-ante games, seeding opening `BettingRound.CurrentBet` from posted blinds, and selecting first preflop actor from the seat after big blind.