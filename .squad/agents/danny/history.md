# Danny History

## Project Learnings (from import)
- Project stack: C#/.NET (`net10.0`) with API services and domain modules.
- Requested by: Rob Gibbens.
- Key backend areas: src/CardGames.Poker.Api, src/CardGames.Poker, src/CardGames.Core.

## Learnings
- 2026-02-17 (Leagues design review): Keep Leagues as account-scoped social orchestration (membership/admin/invites/schedule) and keep poker rules/hand flow outside this boundary.
- 2026-02-17 (Leagues design review): Use append-only membership/admin events with current-state projections to support temporal membership, auditability, and safer concurrency handling.
- 2026-02-17 (Leagues design review): MVP invite links should be revocable + expiring token URLs validated server-side, with join idempotency and hashed token storage.
- 2026-02-17 (Leagues design review): MVP supports both season containers and one-off events as scheduling primitives; standings/leaderboards are post-MVP.
- Scribe merged Leagues design-review and backend-design decisions into canonical `.ai-team/decisions.md`; treat that file as the team-approved MVP contract baseline.
- 2026-02-18 (#216/#224 sync): backend Leagues v1 governance endpoints (transfer ownership, demote admin, remove member) and new membership-history values are now canonicalized in `.ai-team/decisions.md`; API changes should preserve manager/governance-capable invariants and existing moderation/idempotency response semantics expected by integration gates.
- 2026-02-19 (league invite permissions): keep persisted creator membership as `Owner` for governance/audit semantics, but project current-user `Owner` as `Manager` in create/detail/my-leagues responses so management-capable clients remain aligned with invite authorization checks and newly created leagues can create invites immediately from detail flows.
- 2026-02-19 (league invite permissions follow-up): client gating for invite/management actions should treat `Owner` as management-capable alongside `Manager`/`Admin` to stay resilient if any response path returns unprojected role values.
- 2026-02-19 (team update): #216 authority model is now finalized—active admins and managers can execute admin promote/demote operations; ownership transfer remains manager-only with governance safety invariants unchanged.
- 2026-03-02 (Dealer's Choice design): Produced comprehensive API + DB schema design for Dealer's Choice tables. Key files analyzed: `Game.cs` (entity), `GameType.cs`, `CreateGameCommand.cs`/Handler, `ContinuousPlayBackgroundService.cs`, `StartHandCommandHandler.cs`, `IGameFlowHandler.cs`/Factory, `GameHub.cs`, `GameStateBroadcaster.cs`, `TableStatePublicDto.cs`, `Phases.cs`, `PokerGameMetadataRegistry.cs`.
- 2026-03-02 (Dealer's Choice design): Core approach: nullable `GameTypeId` on `Game`, new `WaitingForDealerChoice` phase, `ChooseDealerGameCommand` MediatR command, separate `DealersChoiceDealerPosition` for multi-hand game support, `DealersChoiceHandLog` entity for per-hand audit trail, DC branch in `ContinuousPlayBackgroundService`.
- 2026-03-02 (Dealer's Choice design): Backward compat guaranteed — existing tables always have `IsDealersChoice = false` + non-null `GameTypeId`. All existing flows guarded by early-return check. Phase-based blocking (`WaitingForDealerChoice`) reuses existing action timer/broadcast infrastructure.
- 2026-03-02 (Dealer's Choice design): Multi-hand games (Kings and Lows): `DealersChoiceDealerPosition` stays fixed while sub-hands rotate `DealerPosition`. DC position advances only after full game resolution (showdown with pot awarded, not carried-over pots).
- 2026-03-02 (Dealer's Choice Phase 1 — schema): Applied all DB schema changes: nullable `GameTypeId`/`GameType` on `Game.cs`, added `IsDealersChoice`, `CurrentHandGameTypeCode`, `DealersChoiceDealerPosition` properties. Created `DealersChoiceHandLog` entity + EF config. Added `WaitingForDealerChoice` to `Phases` enum. Made GameType FK optional in `GameConfiguration.cs`. Added `DealersChoiceHandLogs` DbSet to `CardsDbContext`.
- 2026-03-02 (Dealer's Choice Phase 1 — blockers): 3 build errors from nullable `GameTypeId`: `GetActiveGamesMapper.cs:16` (Guid? → Guid), `LobbyStateBroadcastingBehavior.cs:130` (Guid? → Guid), `GetGameMapper.cs:19` (Guid? → Guid). These must be fixed in Phase 2 before migration can be generated.
- 2026-03-05 (PRD 4.11 — DC modal blind support): Added optional `SmallBlind`/`BigBlind` to the full ChooseDealerGame pipeline: Request → Command → Endpoint → Handler → Successful response → DealersChoiceHandLog entity → Contracts DTO → DealerChoiceModal.razor → TablePlay.razor. Blind validation added in handler (must be > 0, BigBlind >= SmallBlind). Frontend conditionally renders blind fields when `HOLDEM` is selected via `IsBlindBasedGame()` helper. Game entity already had `SmallBlind`/`BigBlind` columns — no migration needed. Contracts types live in `CardGames.Poker.Api.Clients` namespace (`ChooseDealerGameExtensions.cs`), not the API namespace.
